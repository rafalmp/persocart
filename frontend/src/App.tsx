// Scaffold shell — routing and real views are added during Implement.
export function App() {
  return (
    <main className="min-h-screen flex items-center justify-center bg-surface text-text-primary">
      <h1 className="text-2xl font-semibold">persocart — scaffold ready</h1>
    </main>
  );
}
